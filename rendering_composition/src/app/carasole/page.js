
import React, { useContext } from 'react';
import { ThemeContext } from '../components/theme-provider';

const Carasoled = () => {

    return (
        <div>
            <p>This is error</p>
        </div>
    );
};

export default Carasoled;