import { Button } from '@headlessui/react'

export default function  Buttons() {
  return (
    <Button className="rounded bg-[#fde825] py-3 px-4 text-sm text-black data-[hover]:bg-sky-500 data-[active]:bg-sky-700">
      Save changes
    </Button>
  )
}